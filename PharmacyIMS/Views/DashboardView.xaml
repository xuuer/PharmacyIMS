<UserControl x:Class="PharmacyIMS.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">
            <TextBlock Text="📊 经营概览" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" Foreground="#2C3E50"/>

            <WrapPanel Margin="0,0,0,20">
                <Border Background="#3498DB" CornerRadius="6" Padding="20,14" Margin="0,0,12,12" MinWidth="160">
                    <StackPanel>
                        <TextBlock Text="药品总数" Foreground="White" FontSize="13" Opacity="0.9"/>
                        <TextBlock Text="{Binding TotalMedicineCount}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="#27AE60" CornerRadius="6" Padding="20,14" Margin="0,0,12,12" MinWidth="160">
                    <StackPanel>
                        <TextBlock Text="库存总价值" Foreground="White" FontSize="13" Opacity="0.9"/>
                        <TextBlock Text="{Binding TotalStockValue,StringFormat={}{0:N0}}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="#9B59B6" CornerRadius="6" Padding="20,14" Margin="0,0,12,12" MinWidth="160">
                    <StackPanel>
                        <TextBlock Text="今日采购金额" Foreground="White" FontSize="13" Opacity="0.9"/>
                        <TextBlock Text="{Binding TodayPurchaseAmount,StringFormat={}{0:N2}}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="#E74C3C" CornerRadius="6" Padding="20,14" Margin="0,0,12,12" MinWidth="160">
                    <StackPanel>
                        <TextBlock Text="今日销售金额" Foreground="White" FontSize="13" Opacity="0.9"/>
                        <TextBlock Text="{Binding TodaySaleAmount,StringFormat={}{0:N2}}" Foreground="White" FontSize="28" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="6" Padding="14" Margin="0,0,8,0" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="⚠ 库存预警药品" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" Foreground="#E74C3C"/>
                        <DataGrid AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal" ItemsSource="{Binding LowStockMedicines}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="药品名称" Width="*" Binding="{Binding MedicineName}"/>
                                <DataGridTextColumn Header="库存" Width="60" Binding="{Binding StockQuantity}"/>
                                <DataGridTextColumn Header="预警值" Width="70" Binding="{Binding StockAlertLevel}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock Text="{Binding LowStockCount, StringFormat=共 {0} 种药品库存不足}" FontSize="12" Foreground="#7F8C8D" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="White" CornerRadius="6" Padding="14" Margin="8,0,0,0" BorderBrush="#E0E0E0" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="📅 临期药品提醒" FontSize="14" FontWeight="Bold" Margin="0,0,0,10" Foreground="#F39C12"/>
                        <DataGrid AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" HeadersVisibility="Column"
                                  GridLinesVisibility="Horizontal" ItemsSource="{Binding NearExpiryMedicines}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="药品名称" Width="*" Binding="{Binding MedicineName}"/>
                                <DataGridTextColumn Header="有效期" Width="100" Binding="{Binding ExpiryDate,StringFormat={}{0:yyyy-MM-dd}}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                        <TextBlock Text="{Binding NearExpiryCount, StringFormat=共 {0} 种药品即将过期}" FontSize="12" Foreground="#7F8C8D" Margin="0,8,0,0"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
