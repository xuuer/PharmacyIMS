<UserControl x:Class="PharmacyIMS.Views.InventoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <DockPanel>
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Border Background="#3498DB" CornerRadius="4" Padding="12,8" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="药品总数" Foreground="White" FontSize="12"/>
                    <TextBlock Text="{Binding TotalMedicineCount}" Foreground="White" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="#E74C3C" CornerRadius="4" Padding="12,8" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="库存预警" Foreground="White" FontSize="12"/>
                    <TextBlock Text="{Binding LowStockCount}" Foreground="White" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>
            <Border Background="#F39C12" CornerRadius="4" Padding="12,8" Margin="0,0,10,0">
                <StackPanel>
                    <TextBlock Text="临期药品" Foreground="White" FontSize="12"/>
                    <TextBlock Text="{Binding NearExpiryCount}" Foreground="White" FontSize="20" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="220" Height="32" Padding="6,4" FontSize="14" VerticalAlignment="Center"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
            <CheckBox Content="仅显示库存预警" Margin="12,0,0,0" VerticalAlignment="Center" IsChecked="{Binding ShowLowStockOnly}"/>
            <CheckBox Content="仅显示临期药品" Margin="12,0,0,0" VerticalAlignment="Center" IsChecked="{Binding ShowNearExpiryOnly}"/>
            <Button Content="🔍 搜索" Width="80" Height="32" Margin="8,0,0,0" FontSize="14" Command="{Binding SearchCommand}"/>
            <Button Content="🔄 刷新" Width="80" Height="32" Margin="8,0,0,0" FontSize="14" Command="{Binding RefreshCommand}"/>
        </StackPanel>

        <DataGrid AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  SelectionMode="Single" AlternatingRowBackground="#F5F7FA"
                  GridLinesVisibility="Horizontal" HeadersVisibility="Column"
                  ItemsSource="{Binding Medicines}" SelectedItem="{Binding SelectedMedicine}">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                            <Setter Property="Background" Value="#FFEBEE"/>
                            <Setter Property="Foreground" Value="#C62828"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsNearExpiry}" Value="True">
                            <Setter Property="Background" Value="#FFF8E1"/>
                            <Setter Property="Foreground" Value="#EF6C00"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="药品编码" Width="90" Binding="{Binding MedicineCode}"/>
                <DataGridTextColumn Header="药品名称" Width="*" Binding="{Binding MedicineName}"/>
                <DataGridTextColumn Header="分类" Width="90" Binding="{Binding Category.CategoryName}"/>
                <DataGridTextColumn Header="库存" Width="60" Binding="{Binding StockQuantity}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="预警下限" Width="80" Binding="{Binding StockAlertLevel}"/>
                <DataGridTextColumn Header="进货价" Width="80" Binding="{Binding PurchasePrice,StringFormat={}{0:N2}}"/>
                <DataGridTextColumn Header="销售价" Width="80" Binding="{Binding SalePrice,StringFormat={}{0:N2}}"/>
                <DataGridTextColumn Header="有效期" Width="100" Binding="{Binding ExpiryDate,StringFormat={}{0:yyyy-MM-dd}}"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock DockPanel.Dock="Bottom" Text="📦 批次信息（点击上方药品查看）" FontSize="14" FontWeight="Bold" Margin="0,10,0,6" Foreground="#2C3E50"/>
        <DataGrid AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  AlternatingRowBackground="#F5F7FA" GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column" ItemsSource="{Binding Batches}" Height="150">
            <DataGrid.Columns>
                <DataGridTextColumn Header="批次号" Width="*" Binding="{Binding BatchNo}"/>
                <DataGridTextColumn Header="数量" Width="80" Binding="{Binding Quantity}"/>
                <DataGridTextColumn Header="单价" Width="90" Binding="{Binding UnitPrice,StringFormat={}{0:N2}}"/>
                <DataGridTextColumn Header="生产日期" Width="110" Binding="{Binding ProductionDate,StringFormat={}{0:yyyy-MM-dd}}"/>
                <DataGridTextColumn Header="有效期" Width="110" Binding="{Binding ExpiryDate,StringFormat={}{0:yyyy-MM-dd}}"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
