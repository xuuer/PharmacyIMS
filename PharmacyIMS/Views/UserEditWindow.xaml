<Window x:Class="PharmacyIMS.Views.UserEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="400" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="用户名：" VerticalAlignment="Center" Margin="0,6"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding User.Username}" Height="28" Padding="4,2"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="密  码：" VerticalAlignment="Center" Margin="0,6"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding PlainPassword, UpdateSourceTrigger=PropertyChanged}" Height="28" Padding="4,2"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="姓  名：" VerticalAlignment="Center" Margin="0,6"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding User.RealName}" Height="28" Padding="4,2"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="角  色：" VerticalAlignment="Center" Margin="0,6"/>
        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding Roles}" SelectedItem="{Binding User.Role}" Height="28"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="电  话：" VerticalAlignment="Center" Margin="0,6"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding User.Phone}" Height="28" Padding="4,2"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="是否启用：" VerticalAlignment="Center" Margin="0,6"/>
        <CheckBox Grid.Row="5" Grid.Column="1" IsChecked="{Binding User.IsActive}" VerticalAlignment="Center"/>

        <TextBlock Grid.Row="6" Grid.Column="1" Text="编辑时密码留空表示不修改" FontSize="11" Foreground="#7F8C8D" VerticalAlignment="Center"/>

        <StackPanel Grid.Row="7" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="保存" Width="100" Height="34" Background="#27AE60" Foreground="White" BorderThickness="0"
                    Command="{Binding SaveCommand}" Margin="0,0,10,0"/>
            <Button Content="取消" Width="100" Height="34" Background="#95A5A6" Foreground="White" BorderThickness="0"
                    Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>
